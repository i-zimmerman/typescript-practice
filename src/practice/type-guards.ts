// TYPE GUARDS

// for primitives -> if typeof

// for objective types -> is prop in object

// more elegant -> is instance of class

// DISCRIMINATED UNION

// adding some common property between types to type guard by this common property
